import * as React from 'react';
import { ArrayFieldBase } from 'ra-core';
/**
 * Renders an embedded array of objects.
 *
 * ArrayField creates a ListContext with the field value, and renders its
 * children components - usually iterator components.
 *
 * @example
 * const PostShow = () => (
 *    <Show>
 *       <SimpleShowLayout>
 *           <ArrayField source="tags">
 *              <SingleFieldList>
 *                 <ChipField source="name" />
 *             </SingleFieldList>
 *           </ArrayField>
 *      </SimpleShowLayout>
 *   </Show>
 * );
 *
 * @see useListContext
 */
export const ArrayField = (props) => React.createElement(ArrayFieldBase, { ...props });
//# sourceMappingURL=ArrayField.js.map